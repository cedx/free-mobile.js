addons: {firefox: latest}
dist: xenial
env: [PLATFORM=node] # TODO PLATFORM=browser
language: node_js
node_js: node
notifications: {email: false}

install:
  - npm install --global gulp-cli
  - npm install

script: gulp test:${PLATFORM}
after_success: npm run-script coverage
